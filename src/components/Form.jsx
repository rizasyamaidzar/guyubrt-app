import React, { useState } from 'react'
const [getter, setter] = stateHandler ?? useState < typeStateForm > ({})
const Form = (field) => {
    return (
        <div>

        </div>
    )
}

export default Form
